{
  "name": "sns-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm start --prefix backend\" \"npm start --prefix frontend\"",
    "build": "react-scripts build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^6.9.15",
    "react-scripts": "^5.0.1"
  }
}
